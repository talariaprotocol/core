export enum MetamapStatusEnum {
    WAITING = 'waiting',
    VERIFIED = 'verified',
    REJECTED = 'rejected',
    REVIEW_NEEDED = 'review_needed',
    EXPIRED = 'expired'
}

export enum UserStatus {
    created= "CREATED",
    done= "DONE",
    error= "ERROR",
    pending_validation= "PENDING_VALIDATION"
}